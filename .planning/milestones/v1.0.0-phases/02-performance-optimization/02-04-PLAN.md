---
phase: 02
plan: 02-04
wave: 4
depends_on: [02-03]
gap_closure: true
requirements_addressed: [N1.4]
autonomous: true
estimated_hours: 1
---

# Plan 02-04: Fix Performance Stats Command Issues

## Objective
Fix the issues found in UAT for the "Show Performance Stats" command.

## Background
UAT found 2 issues:
1. Command title not internationalized (hardcoded "Show Performance Stats" instead of using %key%)
2. Command not found error when executed

---

## Task 1: Add internationalization for command title

<action>
1. Add to `l10n/bundle.l10n.json`:
   ```json
   "command.showPerformanceStats.title": "Show Performance Stats"
   ```

2. Update `package.json` command title:
   ```json
   {
     "command": "mybatis-helper.showPerformanceStats",
     "title": "%command.showPerformanceStats.title%",
     "category": "MyBatis Helper",
     "icon": "$(pulse)"
   }
   ```
</action>

<acceptance_criteria>
- l10n/bundle.l10n.json contains command.showPerformanceStats.title key
- package.json uses %command.showPerformanceStats.title% for the command title
</acceptance_criteria>

---

## Task 2: Verify command registration

<read_first>
- src/extension.ts (check command registration)
- src/commands/showPerformanceStats.ts (check export)
- src/commands/index.ts (check export)
</read_first>

<action>
Verify command registration chain:

1. Check `src/commands/showPerformanceStats.ts` exports the function correctly
2. Check `src/commands/index.ts` exports `showPerformanceStatsCommand`
3. Check `src/extension.ts` imports and registers the command

If any link is broken, fix it.
</action>

<acceptance_criteria>
- Command is properly exported from commands module
- Command is registered in extension.ts with correct command ID
- No TypeScript compilation errors
</acceptance_criteria>

---

## Verification

```bash
# Compile TypeScript
pnpm run compile

# Run unit tests
pnpm run test:unit

# Verify l10n key exists
grep -n "showPerformanceStats" l10n/bundle.l10n.json package.json
```

## must_haves

- [ ] Command title internationalized
- [ ] Command registration verified
- [ ] All tests pass
