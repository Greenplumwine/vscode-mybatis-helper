---
phase: 04-feature-completion
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - src/services/template/templateEngine.ts
  - src/utils/stringUtils.ts
  - src/commands/generateXmlMethod.ts
autonomous: true
requirements:
  - TEMPLATE-01
  - TEMPLATE-02
must_haves:
  truths:
    - "表名推断正确处理 'By' 后缀"
    - "selectByXxx 方法生成 WHERE 子句提示"
    - "复杂返回类型生成正确的 resultMap 引用"
    - "生成的代码无需修改即可编译"
  artifacts:
    - path: "src/services/template/templateEngine.ts"
      provides: "Enhanced template strategies"
      exports: ["SelectTemplateStrategy", "inferTableName"]
    - path: "src/utils/stringUtils.ts"
      provides: "Table name inference utilities"
      exports: ["extractTableNameFromMethod"]
    - path: "src/commands/generateXmlMethod.ts"
      provides: "ResultMap reference generation"
      contains: "generateResultMapRef"
  key_links:
    - from: "SelectTemplateStrategy"
      to: "stringUtils.extractTableNameFromMethod"
      via: "table name inference"
      pattern: "extractTableName|stripBySuffix"
    - from: "generateXmlMethod"
      to: "TemplateEngine"
      via: "resultMap reference"
      pattern: "resultMap.*id|returnType"
---

<objective>
改进代码生成模板，提升生成代码的质量和准确性。

**目的：** 使生成的 MyBatis XML 代码更加智能和完整，减少用户手动修改的工作量，确保生成的代码可以直接编译使用。

**输出：**
- 改进的表名推断逻辑（正确处理 By 后缀）
- 智能 SQL 结构生成（WHERE 子句提示）
- 正确的 resultMap 引用生成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-feature-completion/04-CONTEXT.md

## Key Interfaces

From src/services/template/templateEngine.ts:
```typescript
class SelectTemplateStrategy extends SqlStatementTemplateStrategy {
  render(context: TemplateContext): string
  private convertToTableName(methodName: string): string
}

interface TemplateContext {
  methodName?: string;
  parameters?: JavaParameter[];
  returnType?: string;
  namespace?: string;
}
```

From src/utils/stringUtils.ts:
```typescript
export function camelToSnakeCase(str: string): string
export function removePrefix(str: string, prefix: string, caseInsensitive?: boolean): string
```

Current table name inference:
```typescript
private convertToTableName(methodName: string): string {
  const prefixes = ['select', 'find', 'get', 'query', 'search', 'list'];
  let tableName = methodName;

  for (const prefix of prefixes) {
    const removed = removePrefix(tableName, prefix, true);
    if (removed !== tableName) {
      tableName = removed;
      break;
    }
  }

  const finalName = tableName || methodName;
  return camelToSnakeCase(finalName);
}
// Result: findByUserIdAndStatus → by_user_id_and_status (WRONG)
// Expected: findByUserIdAndStatus → user (or user_id_and_status)
```

From src/commands/generateXmlMethod.ts:
```typescript
export async function generateXmlMethod(
  javaFilePath: string,
  methodName: string,
  templateType: TemplateType
): Promise<void>
```
</context>

<tasks>

<task type="auto">
  <name>任务 1: 改进表名推断逻辑</name>
  <files>src/utils/stringUtils.ts, src/services/template/templateEngine.ts</files>
  <action>
改进表名推断，正确处理 "By" 后缀 (per D-06)：

1. 在 stringUtils.ts 中添加新的表名提取函数：
   ```typescript
   /**
    * 从方法名提取表名
    *
    * 规则：
    * 1. 去除前缀：select/find/get/query/search/list/insert/update/delete/count
    * 2. 去除 "By" 及之后的所有内容
    * 3. 将 CamelCase 转换为 snake_case
    *
    * 示例：
    * - findByUserIdAndStatus → user
    * - selectUserById → user
    * - getUserOrderListByUserId → user_order
    * - insertUser → user
    */
   export function extractTableNameFromMethod(methodName: string): string {
     const prefixes = [
       'select', 'find', 'get', 'query', 'search', 'list',
       'insert', 'update', 'delete', 'count'
     ];

     let result = methodName;

     // 步骤 1: 去除前缀
     for (const prefix of prefixes) {
       const removed = removePrefix(result, prefix, true);
       if (removed !== result) {
         result = removed;
         break;
       }
     }

     // 步骤 2: 去除 "By" 及之后的内容（不区分大小写）
     const byIndex = result.toLowerCase().indexOf('by');
     if (byIndex > 0) {
       result = result.substring(0, byIndex);
     }

     // 步骤 3: 处理 "List" 后缀（常见情况：getUserList → user）
     if (result.toLowerCase().endsWith('list')) {
       result = result.substring(0, result.length - 4);
     }

     // 步骤 4: 转换为 snake_case
     result = camelToSnakeCase(result);

     // 步骤 5: 清理（去除首尾下划线）
     result = result.replace(/^_+|_+$/g, '');

     return result || methodName;
   }
   ```

2. 更新所有模板策略使用新函数：
   - SelectTemplateStrategy
   - InsertTemplateStrategy
   - UpdateTemplateStrategy
   - DeleteTemplateStrategy

3. 添加单元测试：
   ```typescript
   describe('extractTableNameFromMethod', () => {
     test('findByUserIdAndStatus → user', () => {
       expect(extractTableNameFromMethod('findByUserIdAndStatus')).toBe('user');
     });
     test('selectUserById → user', () => {
       expect(extractTableNameFromMethod('selectUserById')).toBe('user');
     });
     test('getUserOrderListByUserId → user_order', () => {
       expect(extractTableNameFromMethod('getUserOrderListByUserId')).toBe('user_order');
     });
   });
   ```
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="stringUtils" --testNamePattern="extractTableName|tableName"</automated>
  </verify>
  <done>
    - extractTableNameFromMethod 函数实现完成
    - 正确处理 "By" 后缀
    - 单元测试覆盖主要场景
  </done>
</task>

<task type="auto">
  <name>任务 2: 实现智能 SQL 结构生成</name>
  <files>src/services/template/templateEngine.ts</files>
  <action>
为 selectByXxx 方法生成更智能的 SQL 结构 (per D-06)：

1. 分析方法名提取查询条件：
   ```typescript
   /**
    * 从方法名提取查询条件
    *
    * 示例：
    * - findByUserIdAndStatus → [{field: 'user_id', operator: '='}, {field: 'status', operator: '='}]
    * - selectByCreateTimeBetween → [{field: 'create_time', operator: 'BETWEEN'}]
    * - getByNameLike → [{field: 'name', operator: 'LIKE'}]
    */
   private extractWhereConditions(methodName: string): WhereCondition[] {
     const byIndex = methodName.toLowerCase().indexOf('by');
     if (byIndex === -1) return [];

     const conditionPart = methodName.substring(byIndex + 2);
     // 按 "And" 或 "Or" 分割
     const parts = conditionPart.split(/(?=[A-Z])/);

     const conditions: WhereCondition[] = [];
     let currentField = '';

     for (const part of parts) {
       const lowerPart = part.toLowerCase();

       if (lowerPart === 'and' || lowerPart === 'or') {
         if (currentField) {
           conditions.push({
             field: camelToSnakeCase(currentField),
             operator: '=',
             logic: lowerPart.toUpperCase() as 'AND' | 'OR'
           });
           currentField = '';
         }
       } else if (['between', 'like', 'in', 'not', 'null', 'is'].includes(lowerPart)) {
         // 操作符处理
         if (currentField && conditions.length > 0) {
           const lastCondition = conditions[conditions.length - 1];
           lastCondition.operator = this.mapOperator(lowerPart, parts);
         }
       } else {
         currentField += part;
       }
     }

     // 处理最后一个字段
     if (currentField) {
       conditions.push({
         field: camelToSnakeCase(currentField),
         operator: '=',
         logic: 'AND'
       });
     }

     return conditions;
   }
   ```

2. 修改 SelectTemplateStrategy.render 方法：
   ```typescript
   render(context: TemplateContext): string {
     const methodName = escapeXml(context.methodName || '');
     const paramsStr = this.renderParameters(context.parameters);
     const resultStr = this.renderResultType(context.returnType);
     const tableName = extractTableNameFromMethod(context.methodName || '');

     // 提取 WHERE 条件
     const conditions = this.extractWhereConditions(context.methodName || '');

     if (conditions.length > 0) {
       // 生成带 WHERE 提示的 SQL
       const whereClause = conditions.map((c, i) => {
         const prefix = i === 0 ? '' : ` ${c.logic} `;
         return `${prefix}${c.field} ${c.operator} ?`;
       }).join('');

       return `    <select id="${methodName}"${paramsStr}${resultStr}>
        SELECT * FROM ${tableName}
        WHERE ${whereClause}
    </select>`;
     }

     // 默认 SQL（无 By 后缀）
     return `    <select id="${methodName}"${paramsStr}${resultStr}>
        SELECT * FROM ${tableName}
        WHERE
    </select>`;
   }
   ```

3. 支持的操作符映射：
   - Equal: ByName → name = ?
   - Like: ByNameLike → name LIKE ?
   - Between: ByCreateTimeBetween → create_time BETWEEN ? AND ?
   - In: ByIdIn → id IN (...)
   - IsNull/IsNotNull: ByNameIsNull → name IS NULL
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="templateEngine" --testNamePattern="where|condition|selectBy"</automated>
  </verify>
  <done>
    - 智能 WHERE 子句生成实现
    - 支持常见操作符（=, LIKE, BETWEEN, IN）
    - 生成的 SQL 可直接使用
  </done>
</task>

<task type="auto">
  <name>任务 3: 实现 resultMap 引用生成</name>
  <files>src/services/template/templateEngine.ts, src/commands/generateXmlMethod.ts</files>
  <action>
为复杂返回类型生成正确的 resultMap 引用 (per D-06)：

1. 分析返回类型决定 resultType 或 resultMap：
   ```typescript
   private shouldUseResultMap(returnType: string): boolean {
     // 基础类型：直接使用 resultType
     const primitiveTypes = ['int', 'long', 'boolean', 'string', 'integer'];
     if (primitiveTypes.includes(returnType.toLowerCase())) {
       return false;
     }

     // 集合类型：检查泛型参数
     if (returnType.includes('<')) {
       const genericMatch = returnType.match(/<(.*?)>/);
       if (genericMatch) {
         const genericType = genericMatch[1];
         return this.isComplexType(genericType);
       }
     }

     // 复杂对象类型：使用 resultMap
     return this.isComplexType(returnType);
   }

   private isComplexType(typeName: string): boolean {
     // 排除基础类型和包装类
     const simpleTypes = [
       'java.lang.String', 'java.lang.Integer', 'java.lang.Long',
       'java.lang.Boolean', 'java.lang.Double', 'java.lang.Float',
       'int', 'long', 'boolean', 'double', 'float'
     ];
     return !simpleTypes.some(t => typeName.includes(t));
   }
   ```

2. 生成 resultMap 引用：
   ```typescript
   private renderResultMapRef(returnType: string, methodName: string): string {
     if (!this.shouldUseResultMap(returnType)) {
       return this.renderResultType(returnType);
     }

     // 提取简单类名
     const simpleTypeName = this.extractSimpleTypeName(returnType);

     // 生成 resultMap ID
     const resultMapId = `${simpleTypeName}ResultMap`;

     return ` resultMap="${resultMapId}"`;
   }
   ```

3. 在 generateXmlMethod 中检查 resultMap 是否存在：
   ```typescript
   // 如果引用了 resultMap 但不存在，生成提示
   private async checkAndSuggestResultMap(
     xmlPath: string,
     resultMapId: string
   ): Promise<string | null> {
     // 检查 XML 中是否已有该 resultMap
     const content = await fs.readFile(xmlPath, 'utf-8');
     if (content.includes(`id="${resultMapId}"`)) {
       return null; // 已存在
     }

     // 返回生成 resultMap 的建议
     return `<!-- TODO: Add resultMap with id="${resultMapId}" -->`;
   }
   ```

4. 更新 ResultMapTemplateStrategy：
   - 根据返回类型的字段生成初始 result 映射
   - 支持嵌套对象（association/collection）检测
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="templateEngine|generateXmlMethod" --testNamePattern="resultMap|returnType"</automated>
  </verify>
  <done>
    - resultMap 引用生成正确
    - 复杂类型自动使用 resultMap
    - 缺失 resultMap 时给出提示
  </done>
</task>

</tasks>

<verification>
1. **功能验证：**
   - 表名推断正确处理 By 后缀
   - WHERE 子句提示准确反映方法名
   - resultMap 引用正确生成

2. **编译验证：**
   - 生成的代码无需修改即可编译
   - 与现有项目模板兼容

3. **场景验证：**
   - 测试 10+ 种常见方法命名模式
   - 测试复杂返回类型（嵌套对象、集合）
</verification>

<success_criteria>
- 表名推断准确率 > 90%
- 生成的代码编译成功率 > 95%
- WHERE 子句提示准确率 > 85%
- resultMap 引用正确率 > 95%
</success_criteria>

<output>
After completion, create `.planning/phases/04-feature-completion/04-04-SUMMARY.md`
</output>

<risk_areas>
1. **命名约定风险：** 不同团队的方法命名习惯不同，表名推断可能不准确
2. **类型解析风险：** 复杂泛型类型（如 Map<String, List<User>>）处理困难
3. **兼容性风险：** 修改模板可能影响现有用户的代码生成习惯
</risk_areas>
