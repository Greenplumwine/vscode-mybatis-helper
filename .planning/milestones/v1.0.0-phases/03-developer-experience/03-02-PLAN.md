---
phase: 03-developer-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/features/welcome/welcomePage.ts
  - src/features/welcome/welcomeContent.ts
  - src/commands/configurationWizard.ts
  - src/extension.ts
  - package.json
  - l10n/bundle.l10n.json
autonomous: false
requirements:
  - ONB-01
  - ONB-02
  - CFG-03
must_haves:
  truths:
    - Welcome page displays on first install
    - Welcome page shows feature cards and quick setup checklist
    - Welcome page has "Don't show again" checkbox
    - Configuration wizard guides through 4-step setup
    - Wizard saves settings to workspace configuration
  artifacts:
    - path: src/features/welcome/welcomePage.ts
      provides: Welcome page webview implementation
      exports: ["showWelcomePage", "shouldShowWelcomePage"]
    - path: src/features/welcome/welcomeContent.ts
      provides: Welcome page HTML content
      exports: ["getWelcomeContent"]
    - path: src/commands/configurationWizard.ts
      provides: Configuration wizard command
      exports: ["runConfigurationWizard"]
    - path: package.json
      provides: Command registrations
      contains: "mybatis-helper.showWelcomePage"
    - path: l10n/bundle.l10n.json
      provides: Internationalized strings
      contains: "welcome.title"
  key_links:
    - from: src/extension.ts
      to: src/features/welcome/welcomePage.ts
      via: import and activation check
      pattern: "showWelcomePage"
    - from: src/features/welcome/welcomePage.ts
      to: src/commands/configurationWizard.ts
      via: "Configure" button handler
      pattern: "runConfigurationWizard"
    - from: src/features/welcome/welcomeContent.ts
      to: l10n/bundle.l10n.json
      via: vscode.l10n.t() calls
      pattern: "welcome\\."
---

<objective>
Implement welcome page and configuration wizard for first-time user onboarding.

Purpose: Guide new users through initial setup and provide a visually appealing entry point to the extension.
Output: VS Code webview welcome page that shows on first install, and a multi-step configuration wizard for setting up the extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-developer-experience/03-CONTEXT.md
@/Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/src/extension.ts
@/Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/package.json
@/Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/l10n/bundle.l10n.json
@/Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/samples/basic-mybatis-project/pom.xml

## Reference: VS Code Webview Patterns

From INTEGRATIONS.md (if exists):
- Use vscode.window.createWebviewPanel()
- Set localResourceRoots for security
- Use webview.asWebviewUri() for local resources
- Post messages between webview and extension

## Reference: QuickPick Wizard Pattern

Multi-step wizard using vscode.window.showQuickPick():
1. Each step returns Thenable that chains to next
2. Use step counter in placeholder text
3. Allow back navigation with undefined check
4. Store answers in state object
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create welcome page webview</name>
  <files>
    src/features/welcome/welcomePage.ts
    src/features/welcome/welcomeContent.ts
    src/features/welcome/index.ts
  </files>
  <read_first>
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/src/extension.ts
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/src/utils/logger.ts
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/l10n/bundle.l10n.json
  </read_first>
  <action>
Create welcome page feature in src/features/welcome/:

1. src/features/welcome/welcomePage.ts:
   - Export function `showWelcomePage(context: vscode.ExtensionContext)`
   - Create webview panel with vscode.ViewColumn.One
   - Title: "Welcome to MyBatis Helper" (use l10n)
   - Enable scripts: true
   - Set localResourceRoots to extension URI
   - Handle message passing from webview:
     - "openSample" -> open sample project folder
     - "configure" -> run configuration wizard
     - "openDocs" -> open docs/FEATURES.md
     - "dontShowAgain" -> set global state flag
   - Export function `shouldShowWelcomePage(context): boolean`
     - Check global state key "mybatis-helper.welcomeShown"
     - Return true if not set or if showWelcome setting is true

2. src/features/welcome/welcomeContent.ts:
   - Export function `getWelcomeContent(webview: vscode.Webview, extensionUri: vscode.Uri): string`
   - Return complete HTML with:
     - Header: "Welcome to MyBatis Helper" with icon
     - Feature cards (3 columns):
       - Navigate: Java to XML bidirectional navigation icon + description
       - Capture: SQL interception icon + description
       - Complete: Code completion icon + description
     - Quick Setup checklist:
       - Checkbox: Java extension installed (auto-checked via redhat.java detection)
       - Checkbox: Mapper files detected (auto-checked via FastMappingEngine)
       - Checkbox: SQL interceptor configured (auto-checked via config)
     - Action buttons:
       - "Open Sample Project" -> samples/basic-mybatis-project/
       - "Configure MyBatis Helper" -> triggers wizard
       - "View Documentation" -> opens docs/FEATURES.md
     - Footer checkbox: "Don't show this page on startup"
   - Use VS Code CSS variables for theming (--vscode-editor-background, etc.)
   - Include inline styles (no external CSS file needed)
   - All user-facing strings via l10n keys (welcome.title, welcome.feature.navigate, etc.)

3. src/features/welcome/index.ts:
   - Re-export showWelcomePage, shouldShowWelcomePage, getWelcomeContent

Follow VS Code welcome page style:
- Clean, card-based layout
- Use Codicons for icons (via CSS)
- Responsive grid layout
- Subtle shadows and borders
  </action>
  <verify>
    <automated>test -f src/features/welcome/welcomePage.ts && test -f src/features/welcome/welcomeContent.ts && test -f src/features/welcome/index.ts && grep -q "showWelcomePage" src/features/welcome/welcomePage.ts && echo "Welcome page files created"</automated>
  </verify>
  <done>
    - welcomePage.ts exports showWelcomePage and shouldShowWelcomePage
    - welcomeContent.ts exports getWelcomeContent
    - HTML includes feature cards, checklist, action buttons
    - Message handlers for all 4 actions
    - Uses l10n for all strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create configuration wizard command</name>
  <files>
    src/commands/configurationWizard.ts
    src/commands/index.ts
  </files>
  <read_first>
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/src/commands/index.ts
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/src/extension.ts
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/package.json
  </read_first>
  <action>
Create configuration wizard in src/commands/configurationWizard.ts:

Export async function `runConfigurationWizard()` that implements 4-step wizard:

Step 1: Project Type Detection
- Auto-detect: Check for pom.xml (Maven) or build.gradle (Gradle)
- Show QuickPick with detected type pre-selected:
  - Options: "Maven project (detected)", "Gradle project", "Other"
  - Placeholder: "Select your project type (Step 1/4)"
- Store selection in wizard state

Step 2: XML Directories Configuration
- Suggest default based on project type:
  - Maven: src/main/resources/mappers/, src/main/resources/mapper/
  - Gradle: src/main/resources/mappers/
  - Other: src/main/resources/
- Show input box with suggestion pre-filled
- Allow adding multiple directories (loop until user selects "Done")
- Placeholder: "Enter XML directory path (Step 2/4)"

Step 3: Naming Convention
- Show QuickPick with preset options:
  - "*Mapper.java -> *Mapper.xml (standard)"
  - "*Dao.java -> *Mapper.xml (DAO style)"
  - "*Mapper.java -> *.xml (simple)"
  - "Custom pattern..."
- If custom selected, show input for pattern
- Placeholder: "Select naming convention (Step 3/4)"

Step 4: SQL Interception Mode
- Show QuickPick:
  - "Auto-detect (recommended)"
  - "Debug Console only"
  - "Terminal only"
- Placeholder: "Select SQL interception mode (Step 4/4)"

After all steps:
- Save configuration to workspace settings using vscode.workspace.getConfiguration()
- Set:
  - mybatis-helper.customXmlDirectories (array from step 2)
  - mybatis-helper.nameMatchingRules (from step 3)
  - mybatis-helper.sqlInterceptor.listenMode (from step 4)
- Show information message: "Configuration saved! Reload window to apply changes?"
- Offer "Reload" and "Later" buttons

Handle cancellation:
- If user escapes at any step, show "Configuration cancelled" message
- Don't save partial configuration

Update src/commands/index.ts:
- Export runConfigurationWizard from configurationWizard
- Add to commands object
  </action>
  <verify>
    <automated>grep -q "runConfigurationWizard" src/commands/configurationWizard.ts && grep -q "configurationWizard" src/commands/index.ts && echo "Configuration wizard created"</automated>
  </verify>
  <done>
    - configurationWizard.ts has 4-step wizard implementation
    - Each step has proper placeholder with step counter
    - Configuration saved to workspace settings
    - Cancellation handled gracefully
    - Exported from commands/index.ts
  </done>
</task>

<task type="auto">
  <name>Task 3: Register commands and integrate welcome page</name>
  <files>
    src/extension.ts
    package.json
    l10n/bundle.l10n.json
  </files>
  <read_first>
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/src/extension.ts
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/package.json
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/src/features/welcome/welcomePage.ts
    /Users/jingzepei/Desktop/myself_code/vscode-mybatis-helper/src/commands/configurationWizard.ts
  </read_first>
  <action>
Integrate welcome page and wizard into extension:

1. Update src/extension.ts:
   - Import { showWelcomePage, shouldShowWelcomePage } from './features/welcome'
   - Import { runConfigurationWizard } from './commands'
   - In activatePluginFeatures(), after initialization:
     - Check shouldShowWelcomePage(context)
     - If true, call showWelcomePage(context)
   - Register new commands in registerCommands():
     - mybatis-helper.showWelcomePage -> showWelcomePage(context)
     - mybatis-helper.configureWizard -> runConfigurationWizard

2. Update package.json contributes.commands:
   Add:
   ```json
   {
     "command": "mybatis-helper.showWelcomePage",
     "title": "%command.showWelcomePage.title%",
     "category": "MyBatis Helper"
   },
   {
     "command": "mybatis-helper.configureWizard",
     "title": "%command.configureWizard.title%",
     "category": "MyBatis Helper"
   }
   ```

3. Update package.json contributes.configuration:
   Add:
   ```json
   "mybatis-helper.showWelcome": {
     "type": "boolean",
     "default": true,
     "description": "%config.showWelcome.description%"
   }
   ```

4. Update l10n/bundle.l10n.json:
   Add welcome-related strings:
   ```json
   "welcome.title": "Welcome to MyBatis Helper",
   "welcome.feature.navigate": "Navigate",
   "welcome.feature.navigate.desc": "Jump between Java and XML with Ctrl+Alt+J/X",
   "welcome.feature.capture": "Capture SQL",
   "welcome.feature.capture.desc": "Intercept and view SQL from debug console",
   "welcome.feature.complete": "Complete",
   "welcome.feature.complete.desc": "Smart code completion in XML files",
   "welcome.setup.title": "Quick Setup",
   "welcome.setup.java": "Java extension installed",
   "welcome.setup.mappers": "Mapper files detected",
   "welcome.setup.sql": "SQL interceptor configured",
   "welcome.action.sample": "Open Sample Project",
   "welcome.action.configure": "Configure MyBatis Helper",
   "welcome.action.docs": "View Documentation",
   "welcome.dontShowAgain": "Don't show this page on startup",
   "command.showWelcomePage.title": "Show Welcome Page",
   "command.configureWizard.title": "Configure MyBatis Helper",
   "config.showWelcome.description": "Show welcome page on startup",
   "wizard.saved": "Configuration saved! Reload window to apply changes?",
   "wizard.cancelled": "Configuration cancelled",
   "wizard.step.projectType": "Select your project type (Step {step}/4)",
   "wizard.step.xmlDirs": "Enter XML directory path (Step {step}/4)",
   "wizard.step.naming": "Select naming convention (Step {step}/4)",
   "wizard.step.sqlMode": "Select SQL interception mode (Step {step}/4)"
   ```

5. Add to all language bundles (l10n/bundle.l10n.*.json):
   - At minimum add English strings to all files
   - Use Google Translate or similar for other languages
  </action>
  <verify>
    <automated>grep -q "showWelcomePage" src/extension.ts && grep -q "mybatis-helper.showWelcomePage" package.json && grep -q "welcome.title" l10n/bundle.l10n.json && echo "Integration complete"</automated>
  </verify>
  <done>
    - extension.ts imports and calls showWelcomePage on activation
    - Commands registered for showWelcomePage and configureWizard
    - package.json has command and configuration entries
    - l10n strings added to all language bundles
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Welcome page and configuration wizard implementation:
1. Welcome page webview with feature cards, setup checklist, and action buttons
2. Configuration wizard with 4-step setup flow
3. Integration with extension activation and command palette
  </what-built>
  <how-to-verify>
1. Build the extension: `pnpm run compile`
2. Press F5 to open Extension Development Host
3. Verify welcome page appears automatically on first load
4. Check welcome page content:
   - 3 feature cards are displayed (Navigate, Capture, Complete)
   - Quick Setup checklist shows status
   - Action buttons work (Open Sample, Configure, View Docs)
5. Test "Don't show again" checkbox and reload
6. Run "MyBatis Helper: Show Welcome Page" from command palette
7. Run "MyBatis Helper: Configure MyBatis Helper" from command palette
8. Complete the 4-step wizard and verify settings are saved
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Welcome page displays on first install
- Welcome page can be reopened from command palette
- Configuration wizard completes all 4 steps
- Settings are saved to workspace configuration
- All UI text is internationalized
</verification>

<success_criteria>
- New user sees welcome page on first extension load
- Welcome page provides clear path to sample project, configuration, and docs
- Configuration wizard guides through setup in < 2 minutes
- Wizard saves valid configuration that works immediately
</success_criteria>

<output>
After completion, create `.planning/phases/03-developer-experience/03-02-SUMMARY.md`
</output>
