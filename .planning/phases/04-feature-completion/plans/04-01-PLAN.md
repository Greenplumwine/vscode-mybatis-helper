---
phase: 04-feature-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/mapping/fastMappingEngine.ts
  - src/services/parsing/javaMethodParser.ts
  - src/features/completion/types.ts
autonomous: true
requirements:
  - COMPLETION-01
  - COMPLETION-02
must_haves:
  truths:
    - "@Param annotation values are correctly extracted during Mapper scanning"
    - "Parameter alias mapping is available for completion provider"
    - "Java Language Server communication is implemented for type resolution"
  artifacts:
    - path: "src/features/mapping/fastMappingEngine.ts"
      provides: "Method parameter parsing with @Param extraction"
      min_lines: 50
    - path: "src/services/parsing/javaMethodParser.ts"
      provides: "JLS type resolution implementation"
      exports: ["getObjectProperties", "resolveTypeFromJLS"]
  key_links:
    - from: "FastMappingEngine.buildMapping"
      to: "JavaMethodParser.parseMethodParameters"
      via: "method parameter extraction"
      pattern: "parseMethodParameters|@Param"
    - from: "PropertyStrategy"
      to: "JavaMethodParser.getObjectProperties"
      via: "JLS type resolution"
      pattern: "executeCommand.*java.getClassMembers"
---

<objective>
实现 Java 类型信息集成，提升代码补全准确性。

**目的：** 通过解析 @Param 注解和与 Java Language Server 通信，提供准确的参数类型信息，使属性补全更加精准。

**输出：**
- 增强的 FastMappingEngine，支持方法参数预解析
- JavaMethodParser 支持 JLS 类型解析
- 参数别名映射缓存机制
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-feature-completion/04-CONTEXT.md

## Key Interfaces

From src/features/completion/types.ts:
```typescript
export interface JavaParameter {
  readonly name: string;
  readonly type: string;
  readonly annotations: string[];
  readonly paramValue?: string;  // @Param alias
}

export interface JavaMethod {
  readonly name: string;
  readonly returnType: string;
  readonly isCollection: boolean;
  readonly parameters: JavaParameter[];
  readonly annotations: string[];
  readonly lineRange: { readonly start: number; readonly end: number };
}
```

From src/features/mapping/fastMappingEngine.ts:
```typescript
interface MappingIndex {
  namespace: string;
  javaPath: string;
  xmlPath?: string;
  className: string;
  simpleClassName: string;
  packageName: string;
  methods: Map<string, MethodMapping>;
  lastUpdated: number;
}
```
</context>

<tasks>

<task type="auto">
  <name>任务 1: 实现 @Param 注解解析</name>
  <files>src/services/parsing/javaMethodParser.ts, src/features/completion/types.ts</files>
  <action>
在 JavaMethodParser 中实现方法参数解析功能，提取 @Param 注解值：

1. 添加 `parseMethodParameters` 方法，解析 Java 方法签名：
   - 使用正则或 AST 解析方法参数列表
   - 识别 @Param("value") 注解并提取别名
   - 返回 JavaParameter[] 数组

2. 示例解析逻辑：
   ```typescript
   // 输入: User selectById(@Param("userId") Long id, @Param("status") Integer status)
   // 输出: [
   //   { name: "id", paramValue: "userId", type: "java.lang.Long", annotations: ["@Param"] },
   //   { name: "status", paramValue: "status", type: "java.lang.Integer", annotations: ["@Param"] }
   // ]
   ```

3. 实现要求 (per D-02)：
   - 支持标准 @Param 注解格式
   - 处理无注解参数（使用参数名作为默认名）
   - 提取参数类型（包括泛型信息如 List<User>）

4. 注意：保持与现有 parseMethod 方法的兼容性
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="javaMethodParser" --testNamePattern="parseMethodParameters|@Param"</automated>
  </verify>
  <done>
    - parseMethodParameters 方法实现完成
    - 能够正确解析带 @Param 注解的方法参数
    - 单元测试覆盖正常情况和边界情况
  </done>
</task>

<task type="auto">
  <name>任务 2: 集成 JLS 类型解析</name>
  <files>src/services/parsing/javaMethodParser.ts</files>
  <action>
实现与 Java Language Server 的通信，获取类型信息：

1. 添加 JLS 通信方法 (per D-01)：
   ```typescript
   private async queryJavaLanguageServer<T>(
     command: string,
     ...args: any[]
   ): Promise<T | null>
   ```

2. 实现 `getObjectProperties` 方法：
   - 使用 `vscode.commands.executeCommand('java.execute.workspaceCommand', ...)`
   - 调用 'java.getClassMembers' 获取类的字段和方法
   - 过滤出 getter 对应的属性名
   - 缓存结果避免重复查询

3. 实现 `resolveTypeFromClasspath` 方法：
   - 解析类路径中的类型（包括 JAR 依赖）
   - 处理泛型类型擦除（List<User> → User）

4. 错误处理：
   - JLS 不可用时降级到简单解析
   - 超时处理（默认 5 秒）
   - 缓存失败结果避免重复超时
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="javaMethodParser" --testNamePattern="getObjectProperties|JLS|resolveType"</automated>
  </verify>
  <done>
    - JLS 通信实现完成
    - getObjectProperties 能够返回类的属性列表
    - 有适当的降级机制和错误处理
  </done>
</task>

<task type="auto">
  <name>任务 3: 在 FastMappingEngine 中缓存参数信息</name>
  <files>src/features/mapping/fastMappingEngine.ts</files>
  <action>
在 FastMappingEngine 中集成方法参数解析和缓存：

1. 扩展 MappingIndex 接口：
   ```typescript
   interface MappingIndex {
     // ... existing fields
     methodParameters?: Map<string, JavaParameter[]>;  // methodName -> parameters
   }
   ```

2. 在 `buildMapping` 方法中添加参数解析：
   - 调用 JavaMethodParser.parseMethodParameters 解析每个方法
   - 将结果存入 methodParameters Map
   - 异步执行，不阻塞映射构建

3. 添加公共方法：
   ```typescript
   public getMethodParameters(javaPath: string, methodName: string): JavaParameter[] | undefined
   ```

4. 性能优化：
   - 参数解析在 worker 线程中执行（参考 classParsingWorker.ts 模式）
   - 延迟加载：首次请求时才解析参数

5. 更新清理逻辑：
   - cleanupStaleEntries 时同时清理 methodParameters
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="fastMappingEngine" --testNamePattern="methodParameters|getMethodParameters"</automated>
  </verify>
  <done>
    - FastMappingEngine 能够缓存方法参数信息
    - getMethodParameters 方法正常工作
    - 参数缓存与主索引生命周期一致
  </done>
</task>

</tasks>

<verification>
1. **功能验证：**
   - @Param 注解正确解析，别名映射准确
   - JLS 通信正常，类型解析准确
   - 参数缓存命中率高

2. **性能验证：**
   - 参数解析不阻塞映射构建
   - JLS 查询有超时保护
   - 缓存避免重复解析

3. **兼容性验证：**
   - 无 @Param 注解的方法使用参数名作为默认名
   - JLS 不可用时降级到简单解析
   - 与现有 PropertyStrategy 集成正常
</verification>

<success_criteria>
- @Param 注解解析准确率 > 95%
- JLS 类型解析响应时间 < 500ms
- 参数缓存命中率 > 80%
- 与现有补全系统无缝集成
</success_criteria>

<output>
After completion, create `.planning/phases/04-feature-completion/04-01-SUMMARY.md`
</output>

<risk_areas>
1. **JLS 依赖风险：** redhat.java 扩展可能未安装或不可用，需要可靠的降级机制
2. **性能风险：** JLS 查询可能较慢，需要异步处理和缓存策略
3. **泛型处理风险：** Java 泛型类型擦除可能导致类型信息丢失
</risk_areas>
